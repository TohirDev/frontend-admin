import { Container } from "@mui/material";

const Navbar = () => {
  return (
    <Container maxWidth="xl">
      <nav>
        <h1>navbar</h1>
      </nav>
    </Container>
  );
};

export default Navbar;
